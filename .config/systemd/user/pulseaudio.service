[Unit]
Description=PulseAudio Sound System

[Service]
Type=dbus
BusName=org.pulseaudio.Server
ExecStart=/usr/bin/pulseaudio --fail
ExecStartPost=/usr/bin/pactl load-module module-x11-publish display=${DISPLAY}
Restart=always
RestartSec=1s

[Install]
RequiredBy=sound.target
